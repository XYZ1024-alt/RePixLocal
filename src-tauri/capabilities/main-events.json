{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-events",
  "description": "Allows the main window to subscribe to backend pipeline events.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
